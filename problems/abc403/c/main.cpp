#include <bits/stdc++.h>
using namespace std;

int main()
{
    cin.tie(nullptr);
    ios::sync_with_stdio(false);

    // ここに解法を書く
    int n, m, q;
    cin >> n >> m >> q;
    int num;
    while (q--)
    {
        cin >> num;
        if (num == 1)
        {
        }
        else if (num == 2)
        {
        }
        else
        {
        }
    }
    return 0;
}
